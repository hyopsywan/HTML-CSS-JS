const a = Number(prompt("Digite um número:"))
const b = Number(prompt("Digite um número:"))
const c = Number(prompt("Digite um número:"))

if (Number.IsNaN (a) || Number.IsNaN (b) || Number.IsNaN (c)) {
    alert("Número Inálido")
} else {
    let (a) maior 
}