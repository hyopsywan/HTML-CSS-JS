//Soma de Numeros N (naturais)

let num = Number(prompt("Digite um número:"))

let soma = 0;

for (let i = 1; i <= num; i++) {
    soma += i;   
}

console.log(`a soma dos numeros de 1 até ${num} é ${soma}`);